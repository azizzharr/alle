import React from 'react';
import ReactDOM from 'react-dom';

const Blog = () =>{
    return (
        const div = {
            border:'1px solid black',
            margin: '2% auto',
            width: '25%',
            height: '550px'

        }
        const h2 = {
            color:'red',
            fontSize:'65px'

        }
        const alle ={
            fontSize:'35px'
        }

    <div style={div} className={container}>
        <h2 style={h2} className={form-group}>
            Sing in
        </h2>
        Username
        <input name="text" placeholder="Username" className={form-control alle form-group}>
            Password
            <input name="password" placeholder="Password" className={form-control alle form-group}>
                <button type="button" className={btn btn-danger form-group alle}>Login</button>


    </div>
    )
}
ReactDom.render(<Blog/>, document.getElementById('root'))